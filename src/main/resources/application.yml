spring:
  application:
    name: seatly

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update   # 개발 초기: update / 운영: validate
    show-sql: true
    # properties:
    #   hibernate:
    #     format_sql: true
